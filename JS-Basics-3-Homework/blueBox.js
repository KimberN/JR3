
//  var symbol = "*"
// function makeTriangle(){
//       for(var i=0; i <=5; i++){
//          var triangle = symbol + i
//          return triangle
//        }
// }
      

//    console.log(makeTriangle(symbol))

   function generatePyramid(num) {
      let number = '';
    
      for (let i = 1; i <= num; i++) {
        console.log(number += i);
      }
    }
    generatePyramid('5');

    
    //---------------------------------------------------------------------------------
    //function generatePyramid() {
      var symbol = '*';
    

      // in a for loop <= the number 5 is not correct
      for (var i = 1; i <= 5; i++) {
        console.log(symbol + symbol);
      }
    
    //generatePyramid(symbol);
    //---------------------------------------------------------------------------
    var sym = '';

    
function getTriangle(sym)
      // in a for loop <= the number 5 is not correct
      for (var i = 1; i <= 5; i++) {
         if(sym===sym){
            console.log('*');
         }
        
      }
      function getTriangle(sym)